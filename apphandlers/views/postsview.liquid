{%extends baseview.liquid%}

{%assign title="Posts"%}

{%block content%}

{%for post in posts%}
  <div class="post-container">
    <div class="post-container-header">
      <div class="post-container-header-title">
        <a class="tiny-link" href="/posts?id={{post.id}}">{{post.title}}</a>
      </div>
    </div>

    <div class="post-container-body">
      {{post.content}}
    </div>
  </div>
{%endfor%}
{%endblock%}
